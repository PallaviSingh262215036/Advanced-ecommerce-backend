spring.application.name=user_management
# ===============================
# Database Configuration
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/userdb
spring.datasource.username=root
spring.datasource.password=RadhaKrishna@26

# ===============================
# JPA / Hibernate Configuration
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Optional
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# In real system it is false so we are adding because by default spring boot enables it for beggineer to avoid lazyLoading Exception by opening session for long.
spring.jpa.open-in-view=false


